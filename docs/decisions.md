# 결정 이력

이 프로젝트의 주요 결정을 시간순으로 기록한다. `CLAUDE.md`가 "지금 지켜야 할 규칙"이라면,
이 파일은 "왜 그 규칙이 생겼는가"를 설명하는 배경 로그다. 계속 늘어나는 성격이라 별도
파일로 분리했다 — `CLAUDE.md`가 비대해지는 것을 막기 위함.

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026.07.15 | ERPNext(Frappe) 기반 확정, Django+FastAPI 자체 구축안 반려 | 리서치 2건 비교 검토. 근거·반려 사유는 `docs/research/Python_ERP_시스템_설계(대안,미채택).md` 상단 배너 참조 |
| 2026.07.15 | `agents/`·`commands/`·`skills/`를 저장소 최상단에서 `.claude/` 하위로 이동 | 기존 위치는 Claude Code가 인식하지 못해 서브에이전트·슬래시커맨드·스킬 10+9+7개가 전부 비활성 상태였음 |
| 2026.07.15 | `commit_log.md`(로컬-GitHub 커밋 동기화 추적) + `docs/build-log.md`(정식 빌드 이후 시스템 조작 이력) 체계 신설 | 커밋이 실제로 push됐는지 매번 눈으로 확인해야 했던 문제 해소, 향후 bench 조작 이력의 감사 추적성 확보 |
| 2026.07.15 | GPLv3/ERPNext 라이선스·상표 준수 가이드라인 명문화 + 빌드 주요사항 발생 시 commit+push를 사용자 승인 없이 수행하도록 사전 승인 | ERPNext 기반 빌드 착수 확정에 따라 실수로 라이선스/상표를 위반하지 않도록 지금 단계부터 습관화, 빌드 이력이 매번 push 확인을 기다리지 않고 즉시 GitHub에 반영되도록 |
| 2026.07.15 | `CLAUDE.md`를 슬림화 — 결정 이력·GPLv3 상세 가이드라인·커밋/빌드 로그 절차·앱 디렉토리 구조를 `docs/`로 분리하고 경로 참조만 남김 | `CLAUDE.md`는 매 세션 최상단에 로드되는 파일이라 비대해지면 토큰 낭비·주의 분산(Attention Dilution)이 생김 — "매 순간 반드시 지켜야 하는 핵심 규칙"과 "배경/절차/성장하는 로그"를 분리 |
| 2026.07.15 | 요구사항 수집 배포보다 기술 인프라(Docker+bench+babipa_erp 스캐폴딩)를 먼저 진행 | 사용자 확정(질문으로 확인) — 업무 데이터 수집을 기다리지 않아도 되는 병렬 가능 작업이라 판단 |
| 2026.07.15 | `frappe_docker`(공식, MIT)를 `build_erp_m` 밖 sibling 디렉토리 `E:\...\00. Claude Code\frappe_docker`에 클론, git 이력 분리 | 빌드 도구이지 우리 저작물이 아님. `apps/babipa_erp`만 `build_erp_m`에 바인드 마운트해 실제 소스는 이 저장소가 버전관리 |
| 2026.07.15 | 개발 컨테이너 볼륨을 `frappe-bench-data`(네이티브 Docker 볼륨) + `apps/babipa_erp`(Windows 바인드 마운트)로 분리 | Windows 호스트 경로 바인드 마운트가 심볼릭 링크·chmod를 지원하지 않아 `bench init`/`git init`이 실패하는 문제 회피. 근거·재현 방법은 `docs/dev-environment.md` |
| 2026.07.15 | Frappe/ERPNext `version-16`(최신 안정) 채택, `develop`(v17, 미출시) 배제 | `bench init` 기본값(`--frappe-branch` 미지정)이 `develop`로 떨어져 erpnext 설치가 깨지는 것을 발견 → 안정 브랜치 명시로 수정 |
| 2026.07.15 | `babipa_erp`을 `bench new-app --no-git`으로 생성 | 자체 `.git`을 만들면 build_erp_m 안에 nested repo가 생기는 문제 + Windows 바인드 마운트의 chmod 실패 문제를 동시에 회피. 버전관리는 build_erp_m 저장소가 그대로 담당 |
| 2026.07.20 | 딥리서치 2건("ERP 지식정리" v15 기준 + 그 v16 검토·보정)을 `docs/research/`로 이동, 각 문서에 상태 배너 추가 | 기존 리서치 문서 2건과 동일하게 "참고자료" 위치·표기 규칙을 따름. 검토 문서 §4.1(v16 채택 권고)이 2026.07.15 독립 결정과 일치함을 확인 |
| 2026.07.20 | `docs/gplv3-compliance.md`의 "Frappe/ERPNext는 GPLv3" 서술을 "Frappe Framework=MIT, ERPNext 코어=GPL-3.0, AGPL은 미설치 에코시스템 앱에만 해당"으로 정정 | 검토 문서 §3-⑧가 지적한 사실 오류. 라이선스 판단이 걸린 문서라 방치 시 향후 배포 검토 시점에 잘못된 전제로 결정할 위험 |
| 2026.07.20 | `.claude/skills/frappe-v16-breaking-changes/` 신설 (Serial and Batch Bundle 조회 패턴, 정렬 기본값 변경, whitelisted method POST 제약 등) | 검토 문서 §2.3·§3-③가 지적한 v16 갭 — 특히 Serial/Batch Bundle 구조를 모르고 `doc.items[].serial_no`를 직접 읽으면 이력추적 로직이 항상 빈 값을 반환하는데, 실제 백엔드 코딩을 시작하기 전 지금 반영해야 그 시점에 조용히 틀린 코드가 나가는 것을 막을 수 있음 |
| 2026.07.20 | `docs/품목코드_260720.xlsx`(289건) 기준 Serial/Batch 추적성 계획 확정(검토 문서 §4.3) — **완제품(21건)=Serial**, **반제품(125)·원재료(94)·부재료(44)·상품(5)=Batch**, 단 부재료 중 스퀴지 2건(`A6160-00001`·`A6160-00002`, SMT 공정용 소모성 도구로 확인)은 **추적 대상 제외**(Serial/Batch 둘 다 미설정) | 완제품 21건 전부 `검사구분=전수검사`이고 이 중 10건은 실제 매출단가가 있어 HKMC 납품 최종단위임을 확인, 원재료·반제품·부재료는 무검사/샘플링검사 위주라 이미 로트 단위 품질판정이 회사 관행. `A101A-*`(SENSOR SUB ASSY, 반제품)→`A203A-*`(PCB ASSY, 반제품)→`JGP26-*`(SENSOR ASSY, 완제품)로 이어지는 BOM 계층상 완제품에만 Serial을 두는 조합이 전후방 추적에 부합. 스퀴지 제외·`A4001-00007`(RADOME) 상품 분류는 사용자 확인 |
| 2026.07.20 | 검토 문서 §4.5 회계 범위 — **옵션 B(운영 중심 ERP + 세무 이원화)** 확정. ERP는 재고·생산·품질·구매·판매+관리회계까지만 담당, 부가세 신고·전자세금계산서·법인세 등 세무 업무는 기존 **더존(스마트A 등)**을 그대로 유지하고 월마감 시 요약 대사만 수행 | 사용자 확인 결과 더존을 이미 세무 업무에 사용 중임을 확인 — 옵션 A(ERP 풀 재무회계)는 전자세금계산서 발행·부가세 홈택스 신고가 국세청 인증 시스템과의 실시간 연동을 전제하는데, 폐쇄망 인트라넷에서는 이 연동이 §5.1(HKMC EDI)과 동일한 구조적 문제에 부딪혀 결국 별도 수동 브리지가 필요해짐 — "이중 시스템 부담 해소"라는 옵션 A의 목적이 무효화됨. 더존은 세법 개정을 벤더가 매년 자동 반영해 사내 개발 리소스 소모가 없고, 55명·단일고객(HKMC) 구조에서는 월 1회 COA 요약 대사로 충분한 수준이라 이중입력 부담도 크지 않음. 1단계의 "한국 COA·부가세 템플릿"은 신고 목적이 아닌 내부 정합 목적으로 톤 다운해 진행 |
| 2026.07.20 | 검토 문서 §5.4 권한 Role Matrix 초안 확정 — **생산/품질/구매/경리/관리자 5개 역할** × 주요 DocType(Item/BOM, Work Order, Job Card, Stock Entry, Quality Inspection, PPAP/APQP/CP/PFMEA/FAI/4M, Purchase Order/Receipt, Supplier, Sales Order, Delivery Note, GL/Journal/Payment Entry, Stock Ledger)에 RWCD+Submit/Cancel/Amend 매트릭스를 배정. 원칙은 "문서를 만드는 사람"과 "확정(Submit)하는 사람"의 분리 — 생산/품질/구매 실무진은 RWC 위주, 확정 권한은 해당 팀장 또는 관리자 role에 집중. **System Manager는 2인 이내로 제한** — 5개 팀장 전원이 아니라 지정된 2개 팀의 팀장만 겸임하고, 나머지 팀장은 자기 팀 DocType 관리 권한만 가지며 시스템 설정·사용자관리 권한은 없음. Quality Inspection·PPAP/APQP/CP/PFMEA/FAI/4M·Item/BOM·Work Order·GL 관련 문서는 Track Changes ON(감사 추적성). 운영 사이트 Server Script는 비활성 유지(로직은 `babipa_erp` 앱 코드로만) | 사용자 확인 결과 5개 역할 구성이 실제 조직과 일치, System Manager는 "5명 전원(팀장 각자)"이 아닌 "2명만(특정 2개 팀 팀장)" 선택 — 리서치 문서 §5.4가 권고한 "2인 이내" 원칙을 그대로 따름(전산 최종 권한 보유자가 늘어날수록 감사 리스크 증가). 매트릭스 설계는 §4.5에서 확정된 "ERP=관리회계만"(GL/Journal/Payment Entry에 세무 신고 기능 없음, 경리 role의 Submit 대상은 내부 정합용 전표에 한정)과 IATF 8.5.2 감사증적 요구(§4.6 커스텀 DocType Track Changes 원칙)를 그대로 반영. Work Order/Purchase Order 등 트랜잭션 확정 권한은 "팀장급(관리자 role) 전제" 유지로 확인받음 |
| 2026.07.20 | 검토 문서 §5.1 HKMC EDI/VAATZ 연동 방식 — **구조 원칙은 수동 브리지로 확정**(VAATZ 발주·납품지시를 외부망 PC에서 CSV/Excel 추출 → 승인 매체 반입 → ERP Data Import, 역방향도 동일), DMZ 중계 서버·ERP 외부망 이중화는 배제. 단 **실제 "VAATZ Import 매핑" 커스텀 개발은 보류** — 로드맵 3단계(3–6개월, §7) 그대로 유지하고 VAATZ 연동이 정기 채널로 정착하는 시점까지 착수하지 않음 | 폐쇄망 ERP는 정의상 VAATZ 포털과 직접 통신 불가하므로 구조 원칙(수동 브리지)은 연동 정착 여부와 무관하게 이미 결정됨. 사용자 확인 결과 VAATZ 연동이 아직 비정기적 — 실제 CSV 컬럼 구조·발주 주기가 확정 안 된 상태에서 매핑 스크립트를 먼저 만들면 추측 기반 개발이 되어 실제 포맷 확인 후 재작업할 위험이 큼. DMZ 중계 서버는 물량이 커야 투자가 정당화되는데 연동 자체가 비정기적이면 오히려 과잉투자 근거가 더 약해짐(리서치 문서 "물량 증가 시 재검토" 단서에 부합) |
| 2026.07.20 (미결정, 후속 확인 필요) | 검토 문서 §5.5(전자결재 범위)는 사업적 판단이 필요해 아직 결정하지 않음 | 마스터데이터·모듈 설계보다 먼저 확정해야 뒤에서 재작업이 없음(검토 문서 §6.7 체크리스트) — 사용자 확인 후 이 표에 추가 결정으로 기록 예정 |
| 2026.07.20 | Item DocType 필드 매핑 확정(설계 단계) — `품목구분`은 표준 `item_group`, `품목코드`/`항목명`은 표준 `item_code`/`item_name`(임포트는 아직 안 함), `고객품번`은 표준 `customer_items`(Item Customer Detail) 경로로 가되 원본 289건 전부 공백이라 즉시 필요는 없음, `매출/매입/외주단가`는 Item 자체 필드가 아니라 표준 Item Price로, `제조유형`·`검사구분`·`중분류`·`모델`·`규격`·`Array수량`·`작업공정`은 Custom Field 9개(Section/Column Break 2개 포함) 신설 | `docs/item-doctype-design.md` 상세 근거. `제조유형`·`작업공정`은 BOM/Routing 개념에 더 가까워 이번 범위에서는 Custom Field로 원문만 보존하고 정식 BOM/Routing 설계는 별도 세션으로 미룸. Item Group 8건(품목구분 5 + 하위 일반반제품·PCB ASSY(반제품)·PCB ASSY(원재료))도 함께 확정해 개발 사이트에 생성·fixture export 완료 |
| 2026.07.20 (미결정, 후속 확인 필요) | `중분류`의 숫자 레거시 코드 6종(03/09/10/97/98/99)이 정확히 무엇을 뜻하는지 원문에 코드표가 없어 Item Group Level 2로 승격하지 않고 보류, `stock_uom`이 189/289건 공백(필수 필드라 임포트 시 막힘), HKMC Customer 마스터가 개발 사이트에 0건(현재는 `고객품번` 자체가 전부 공백이라 급하지 않음) | `docs/item-doctype-design.md` §1.3·§4·§5. 사용자 확인 후 이 표에 추가 결정으로 기록 예정 |
| 2026.07.20 | `hkmc-compliance` 리뷰 보완 항목 중 안전특성 오버라이드·ES 표준 참조 필드 구조 추가 완료, 값 채우기는 후속 — Item DocType에 Custom Field 2건(`custom_safety_critical` Check/default 0, `custom_es_standard_code`+`custom_es_standard_revision` Data) 신설, 기존 9개와 같은 섹션에 이어붙임 | Item DocType 필드 매핑 리뷰(직전 행)에서 나온 보완 요청 2건: (1) 완제품=Serial/그외=Batch 기본규칙(§ 위 Serial/Batch 행)의 예외 처리 구조가 없어 특별특성(Special Characteristic) 품목을 나중에 개별추적으로 전환할 방법이 없었음 → 플래그 필드로 구조만 우선 확보, 어떤 품목이 해당되는지는 도면 검토가 선행돼야 하므로 판단하지 않음(전부 기본값 0). (2) 검사구분(`custom_inspection_type`)의 근거 ES 표준을 감사 시 즉시 답할 수 있는 필드가 없었음 → `docs/품목코드_260720.xlsx` 원본에 이 정보가 없어 값은 못 채우고 구조만 생성. `docs/item-doctype-design.md` §2·§7 갱신 |
| 2026.07.20 | `docs/품목코드_260720.XLSX`를 `.gitignore`에 등록해 버전관리에서 제외(로컬 디스크엔 유지) — Item DocType 설계 커밋(`107adf7`)에서 아직 원격에 push되지 않은 시점에 사전 제거 | `build_erp_m`은 GitHub **public repo**(`lsy860224/build_erp_m`)로 확인됨. 이 xlsx엔 실제 HKMC向 매출/매입/외주단가·품목코드·차종 매핑이 들어있어 공개 저장소에 올리면 영업기밀 노출. frappe-architect 서브에이전트가 이 파일을 포함해 push를 시도했으나 Claude Code 보안 분류기가 먼저 차단했고, 사용자 확인 후 커밋을 amend해 제외. **향후 원칙**: 실제 영업기밀(단가·고객 매핑 등)이 담긴 원본 데이터 파일은 이 저장소가 public인 한 절대 커밋하지 않는다 — 구조/스키마 설계 문서는 커밋해도 되지만 원본 데이터는 로컬 전용 |
| 2026.07.20 | 완제품 21건(전수검사) Quality Inspection Template 껍데기를 **fixtures가 아니라 patch**(`apps/babipa_erp/babipa_erp/patches/v0_0/create_finished_goods_qi_template_shells.py`)로 구현 | `hkmc-compliance` 리뷰의 "`custom_inspection_type`이 실제 검사기록 생성을 강제하지 않는 라벨뿐" 지적 보완. 실제 검사 파라미터(측정 항목/규격 상하한/AQL)를 담은 도면·Control Plan·검사기준서 원본이 아직 없다고 사용자가 명시 확인 — 값은 채우지 않고 품목코드당 템플릿 1건 + placeholder 파라미터 링크만 생성. `item_quality_inspection_parameter`(Table)가 `reqd=1`이라 빈 테이블은 저장 자체가 막힘을 개발 사이트 실측으로 확인(자식 테이블의 `specification`도 Data가 아니라 `Quality Inspection Parameter` Link이자 `reqd=1`), 그래서 21건이 공유하는 placeholder 마스터 파라미터 1건을 만들어 연결. fixtures 대신 patch를 택한 이유: 이 21건은 Item Group 같은 "앱이 항상 동일하게 유지해야 하는 분류 체계"가 아니라 품목코드 1:1 인스턴스 데이터이고, 품질부서가 도면 확보 후 직접 값을 채워 넣을 대상이라 `bench migrate` 시 fixtures로 재동기화되며 실제 입력값이 덮어써지거나 껍데기로 되돌아갈 위험을 피하기 위함(멱등성 가드 포함, 상세 근거는 `docs/item-doctype-design.md` §6.1). 실 Item 레코드가 아직 없어 `quality_inspection_template` 연결은 완제품 임포트 시점(frappe-backend)으로 위임 |
