# 결정 이력

이 프로젝트의 주요 결정을 시간순으로 기록한다. `CLAUDE.md`가 "지금 지켜야 할 규칙"이라면,
이 파일은 "왜 그 규칙이 생겼는가"를 설명하는 배경 로그다. 계속 늘어나는 성격이라 별도
파일로 분리했다 — `CLAUDE.md`가 비대해지는 것을 막기 위함.

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026.07.15 | ERPNext(Frappe) 기반 확정, Django+FastAPI 자체 구축안 반려 | 리서치 2건 비교 검토. 근거·반려 사유는 `docs/research/Python_ERP_시스템_설계(대안,미채택).md` 상단 배너 참조 |
| 2026.07.15 | `agents/`·`commands/`·`skills/`를 저장소 최상단에서 `.claude/` 하위로 이동 | 기존 위치는 Claude Code가 인식하지 못해 서브에이전트·슬래시커맨드·스킬 10+9+7개가 전부 비활성 상태였음 |
| 2026.07.15 | `commit_log.md`(로컬-GitHub 커밋 동기화 추적) + `docs/build-log.md`(정식 빌드 이후 시스템 조작 이력) 체계 신설 | 커밋이 실제로 push됐는지 매번 눈으로 확인해야 했던 문제 해소, 향후 bench 조작 이력의 감사 추적성 확보 |
| 2026.07.15 | GPLv3/ERPNext 라이선스·상표 준수 가이드라인 명문화 + 빌드 주요사항 발생 시 commit+push를 사용자 승인 없이 수행하도록 사전 승인 | ERPNext 기반 빌드 착수 확정에 따라 실수로 라이선스/상표를 위반하지 않도록 지금 단계부터 습관화, 빌드 이력이 매번 push 확인을 기다리지 않고 즉시 GitHub에 반영되도록 |
| 2026.07.15 | `CLAUDE.md`를 슬림화 — 결정 이력·GPLv3 상세 가이드라인·커밋/빌드 로그 절차·앱 디렉토리 구조를 `docs/`로 분리하고 경로 참조만 남김 | `CLAUDE.md`는 매 세션 최상단에 로드되는 파일이라 비대해지면 토큰 낭비·주의 분산(Attention Dilution)이 생김 — "매 순간 반드시 지켜야 하는 핵심 규칙"과 "배경/절차/성장하는 로그"를 분리 |
| 2026.07.15 | 요구사항 수집 배포보다 기술 인프라(Docker+bench+babipa_erp 스캐폴딩)를 먼저 진행 | 사용자 확정(질문으로 확인) — 업무 데이터 수집을 기다리지 않아도 되는 병렬 가능 작업이라 판단 |
| 2026.07.15 | `frappe_docker`(공식, MIT)를 `build_erp_m` 밖 sibling 디렉토리 `E:\...\00. Claude Code\frappe_docker`에 클론, git 이력 분리 | 빌드 도구이지 우리 저작물이 아님. `apps/babipa_erp`만 `build_erp_m`에 바인드 마운트해 실제 소스는 이 저장소가 버전관리 |
| 2026.07.15 | 개발 컨테이너 볼륨을 `frappe-bench-data`(네이티브 Docker 볼륨) + `apps/babipa_erp`(Windows 바인드 마운트)로 분리 | Windows 호스트 경로 바인드 마운트가 심볼릭 링크·chmod를 지원하지 않아 `bench init`/`git init`이 실패하는 문제 회피. 근거·재현 방법은 `docs/dev-environment.md` |
| 2026.07.15 | Frappe/ERPNext `version-16`(최신 안정) 채택, `develop`(v17, 미출시) 배제 | `bench init` 기본값(`--frappe-branch` 미지정)이 `develop`로 떨어져 erpnext 설치가 깨지는 것을 발견 → 안정 브랜치 명시로 수정 |
| 2026.07.15 | `babipa_erp`을 `bench new-app --no-git`으로 생성 | 자체 `.git`을 만들면 build_erp_m 안에 nested repo가 생기는 문제 + Windows 바인드 마운트의 chmod 실패 문제를 동시에 회피. 버전관리는 build_erp_m 저장소가 그대로 담당 |
| 2026.07.20 | 딥리서치 2건("ERP 지식정리" v15 기준 + 그 v16 검토·보정)을 `docs/research/`로 이동, 각 문서에 상태 배너 추가 | 기존 리서치 문서 2건과 동일하게 "참고자료" 위치·표기 규칙을 따름. 검토 문서 §4.1(v16 채택 권고)이 2026.07.15 독립 결정과 일치함을 확인 |
| 2026.07.20 | `docs/gplv3-compliance.md`의 "Frappe/ERPNext는 GPLv3" 서술을 "Frappe Framework=MIT, ERPNext 코어=GPL-3.0, AGPL은 미설치 에코시스템 앱에만 해당"으로 정정 | 검토 문서 §3-⑧가 지적한 사실 오류. 라이선스 판단이 걸린 문서라 방치 시 향후 배포 검토 시점에 잘못된 전제로 결정할 위험 |
| 2026.07.20 | `.claude/skills/frappe-v16-breaking-changes/` 신설 (Serial and Batch Bundle 조회 패턴, 정렬 기본값 변경, whitelisted method POST 제약 등) | 검토 문서 §2.3·§3-③가 지적한 v16 갭 — 특히 Serial/Batch Bundle 구조를 모르고 `doc.items[].serial_no`를 직접 읽으면 이력추적 로직이 항상 빈 값을 반환하는데, 실제 백엔드 코딩을 시작하기 전 지금 반영해야 그 시점에 조용히 틀린 코드가 나가는 것을 막을 수 있음 |
| 2026.07.20 | `docs/품목코드_260720.xlsx`(289건) 기준 Serial/Batch 추적성 계획 확정(검토 문서 §4.3) — **완제품(21건)=Serial**, **반제품(125)·원재료(94)·부재료(44)·상품(5)=Batch**, 단 부재료 중 스퀴지 2건(`A6160-00001`·`A6160-00002`, SMT 공정용 소모성 도구로 확인)은 **추적 대상 제외**(Serial/Batch 둘 다 미설정) | 완제품 21건 전부 `검사구분=전수검사`이고 이 중 10건은 실제 매출단가가 있어 HKMC 납품 최종단위임을 확인, 원재료·반제품·부재료는 무검사/샘플링검사 위주라 이미 로트 단위 품질판정이 회사 관행. `A101A-*`(SENSOR SUB ASSY, 반제품)→`A203A-*`(PCB ASSY, 반제품)→`JGP26-*`(SENSOR ASSY, 완제품)로 이어지는 BOM 계층상 완제품에만 Serial을 두는 조합이 전후방 추적에 부합. 스퀴지 제외·`A4001-00007`(RADOME) 상품 분류는 사용자 확인 |
| 2026.07.20 | 검토 문서 §4.5 회계 범위 — **옵션 B(운영 중심 ERP + 세무 이원화)** 확정. ERP는 재고·생산·품질·구매·판매+관리회계까지만 담당, 부가세 신고·전자세금계산서·법인세 등 세무 업무는 기존 **더존(스마트A 등)**을 그대로 유지하고 월마감 시 요약 대사만 수행 | 사용자 확인 결과 더존을 이미 세무 업무에 사용 중임을 확인 — 옵션 A(ERP 풀 재무회계)는 전자세금계산서 발행·부가세 홈택스 신고가 국세청 인증 시스템과의 실시간 연동을 전제하는데, 폐쇄망 인트라넷에서는 이 연동이 §5.1(HKMC EDI)과 동일한 구조적 문제에 부딪혀 결국 별도 수동 브리지가 필요해짐 — "이중 시스템 부담 해소"라는 옵션 A의 목적이 무효화됨. 더존은 세법 개정을 벤더가 매년 자동 반영해 사내 개발 리소스 소모가 없고, 55명·단일고객(HKMC) 구조에서는 월 1회 COA 요약 대사로 충분한 수준이라 이중입력 부담도 크지 않음. 1단계의 "한국 COA·부가세 템플릿"은 신고 목적이 아닌 내부 정합 목적으로 톤 다운해 진행 |
| 2026.07.20 | 검토 문서 §5.4 권한 Role Matrix 초안 확정 — **생산/품질/구매/경리/관리자 5개 역할** × 주요 DocType(Item/BOM, Work Order, Job Card, Stock Entry, Quality Inspection, PPAP/APQP/CP/PFMEA/FAI/4M, Purchase Order/Receipt, Supplier, Sales Order, Delivery Note, GL/Journal/Payment Entry, Stock Ledger)에 RWCD+Submit/Cancel/Amend 매트릭스를 배정. 원칙은 "문서를 만드는 사람"과 "확정(Submit)하는 사람"의 분리 — 생산/품질/구매 실무진은 RWC 위주, 확정 권한은 해당 팀장 또는 관리자 role에 집중. **System Manager는 2인 이내로 제한** — 5개 팀장 전원이 아니라 지정된 2개 팀의 팀장만 겸임하고, 나머지 팀장은 자기 팀 DocType 관리 권한만 가지며 시스템 설정·사용자관리 권한은 없음. Quality Inspection·PPAP/APQP/CP/PFMEA/FAI/4M·Item/BOM·Work Order·GL 관련 문서는 Track Changes ON(감사 추적성). 운영 사이트 Server Script는 비활성 유지(로직은 `babipa_erp` 앱 코드로만) | 사용자 확인 결과 5개 역할 구성이 실제 조직과 일치, System Manager는 "5명 전원(팀장 각자)"이 아닌 "2명만(특정 2개 팀 팀장)" 선택 — 리서치 문서 §5.4가 권고한 "2인 이내" 원칙을 그대로 따름(전산 최종 권한 보유자가 늘어날수록 감사 리스크 증가). 매트릭스 설계는 §4.5에서 확정된 "ERP=관리회계만"(GL/Journal/Payment Entry에 세무 신고 기능 없음, 경리 role의 Submit 대상은 내부 정합용 전표에 한정)과 IATF 8.5.2 감사증적 요구(§4.6 커스텀 DocType Track Changes 원칙)를 그대로 반영. Work Order/Purchase Order 등 트랜잭션 확정 권한은 "팀장급(관리자 role) 전제" 유지로 확인받음 |
| 2026.07.20 | 검토 문서 §5.1 HKMC EDI/VAATZ 연동 방식 — **구조 원칙은 수동 브리지로 확정**(VAATZ 발주·납품지시를 외부망 PC에서 CSV/Excel 추출 → 승인 매체 반입 → ERP Data Import, 역방향도 동일), DMZ 중계 서버·ERP 외부망 이중화는 배제. 단 **실제 "VAATZ Import 매핑" 커스텀 개발은 보류** — 로드맵 3단계(3–6개월, §7) 그대로 유지하고 VAATZ 연동이 정기 채널로 정착하는 시점까지 착수하지 않음 | 폐쇄망 ERP는 정의상 VAATZ 포털과 직접 통신 불가하므로 구조 원칙(수동 브리지)은 연동 정착 여부와 무관하게 이미 결정됨. 사용자 확인 결과 VAATZ 연동이 아직 비정기적 — 실제 CSV 컬럼 구조·발주 주기가 확정 안 된 상태에서 매핑 스크립트를 먼저 만들면 추측 기반 개발이 되어 실제 포맷 확인 후 재작업할 위험이 큼. DMZ 중계 서버는 물량이 커야 투자가 정당화되는데 연동 자체가 비정기적이면 오히려 과잉투자 근거가 더 약해짐(리서치 문서 "물량 증가 시 재검토" 단서에 부합) |
| 2026.07.20 | 검토 문서 §5.5 전자결재 범위 확정 — **일반 기안·합의·전결·대결은 ERP Workflow로 옮기지 않고 기존 그룹웨어를 그대로 유지.** ERP 안에는 그룹웨어가 커버하지 않으면서 시스템 자체가 진행을 막아야 하는 트랜잭션 **2개만** Frappe Workflow 적용 — **Purchase Order(발주요청→팀장 승인→확정)**, **Payment Entry(대금 지급 요청→팀장 승인→실행)**. 그 외 DocType은 §5.4에서 확정한 role matrix의 Submit 권한(단일 확정자)만으로 충분, 별도 Workflow 레이어 없음 | Frappe Workflow는 상태전이+단일 승인자 지정 수준이라 한국식 다단계 결재선을 대체할 수 없고, 이미 검증된 그룹웨어를 두고 ERP 안에 열등한 대체품을 또 만드는 건 §4.5(더존 유지)·§5.1(VAATZ 수동 브리지)과 동일한 원칙을 어기는 scope creep. 사용자 확인 결과 발주(PO)·지급(Payment Entry) 둘 다 그룹웨어 기안 절차를 거치지 않고 ERP에서만 처리되고 있어, 이 두 트랜잭션은 §5.4 role matrix의 단일 역할 Submit 권한만으로는(구매/경리 담당자 1인이 만들고 그대로 확정 가능) 사전 승인 게이트가 전혀 없는 상태 — 리서치 문서 권고("핵심 2–3개만 Workflow")의 실제 대상이 이 둘로 확정됨. 그룹웨어 기안을 거치는 다른 트랜잭션에 Workflow를 추가하면 이중승인이 생기므로 대상에서 제외 |
| 2026.07.20 | Item DocType 필드 매핑 확정(설계 단계) — `품목구분`은 표준 `item_group`, `품목코드`/`항목명`은 표준 `item_code`/`item_name`(임포트는 아직 안 함), `고객품번`은 표준 `customer_items`(Item Customer Detail) 경로로 가되 원본 289건 전부 공백이라 즉시 필요는 없음, `매출/매입/외주단가`는 Item 자체 필드가 아니라 표준 Item Price로, `제조유형`·`검사구분`·`중분류`·`모델`·`규격`·`Array수량`·`작업공정`은 Custom Field 9개(Section/Column Break 2개 포함) 신설 | `docs/item-doctype-design.md` 상세 근거. `제조유형`·`작업공정`은 BOM/Routing 개념에 더 가까워 이번 범위에서는 Custom Field로 원문만 보존하고 정식 BOM/Routing 설계는 별도 세션으로 미룸. Item Group 8건(품목구분 5 + 하위 일반반제품·PCB ASSY(반제품)·PCB ASSY(원재료))도 함께 확정해 개발 사이트에 생성·fixture export 완료 |
| 2026.07.20 (미결정, 후속 확인 필요) | `중분류`의 숫자 레거시 코드 6종(03/09/10/97/98/99)이 정확히 무엇을 뜻하는지 원문에 코드표가 없어 Item Group Level 2로 승격하지 않고 보류, `stock_uom`이 189/289건 공백(필수 필드라 임포트 시 막힘), HKMC Customer 마스터가 개발 사이트에 0건(현재는 `고객품번` 자체가 전부 공백이라 급하지 않음) | `docs/item-doctype-design.md` §1.3·§4·§5. 사용자 확인 후 이 표에 추가 결정으로 기록 예정 |
| 2026.07.20 | `hkmc-compliance` 리뷰 보완 항목 중 안전특성 오버라이드·ES 표준 참조 필드 구조 추가 완료, 값 채우기는 후속 — Item DocType에 Custom Field 2건(`custom_safety_critical` Check/default 0, `custom_es_standard_code`+`custom_es_standard_revision` Data) 신설, 기존 9개와 같은 섹션에 이어붙임 | Item DocType 필드 매핑 리뷰(직전 행)에서 나온 보완 요청 2건: (1) 완제품=Serial/그외=Batch 기본규칙(§ 위 Serial/Batch 행)의 예외 처리 구조가 없어 특별특성(Special Characteristic) 품목을 나중에 개별추적으로 전환할 방법이 없었음 → 플래그 필드로 구조만 우선 확보, 어떤 품목이 해당되는지는 도면 검토가 선행돼야 하므로 판단하지 않음(전부 기본값 0). (2) 검사구분(`custom_inspection_type`)의 근거 ES 표준을 감사 시 즉시 답할 수 있는 필드가 없었음 → `docs/품목코드_260720.xlsx` 원본에 이 정보가 없어 값은 못 채우고 구조만 생성. `docs/item-doctype-design.md` §2·§7 갱신 |
| 2026.07.20 | 회계모듈 점검 중 발견한 Company 상태 확정: `BABIPA`(Setup Wizard로 2026.07.15경 이미 생성, 국가 Korea/통화 KRW/CoA `Standard`, 계정 94건)는 실제 법인명이 아니지만 현재는 구축·테스트 단계이므로 이름을 바꾸지 않고 그대로 유지. 함께 있던 `BABIPA (Demo)`(계정 95건 + Sales/Purchase Invoice·Order, Payment Entry, GL Entry 42건 등 실제 트랜잭션 포함)는 정리 대상으로 확정, `Transaction Deletion Record` 표준 메커니즘으로 삭제 완료 | 사용자 확인 결과 실제 법인명 사용은 지금 단계에서 급하지 않음(양산·감사 대응 시점에 재검토) — Company 리네임은 계정과목·전표가 쌓이기 전인 지금이 가장 싸지만, 아직 트랜잭션이 없어 나중에 바꿔도 비용이 크지 않다고 판단해 보류. `BABIPA (Demo)`는 ERPNext 표준 설치/데모 데이터로 추정되며 §4.5(운영 중심 ERP) 결정과 무관하게 실사용 회사와 계정 체계가 섞여 있으면 이후 GL 리포트·역할 매트릭스 배정에서 혼선을 일으킬 수 있어 삭제. 상세 절차는 `docs/build-log.md` 해당 항목 |
| 2026.07.20 | `docs/품목코드_260720.XLSX`를 `.gitignore`에 등록해 버전관리에서 제외(로컬 디스크엔 유지) — Item DocType 설계 커밋(`107adf7`)에서 아직 원격에 push되지 않은 시점에 사전 제거 | `build_erp_m`은 GitHub **public repo**(`lsy860224/build_erp_m`)로 확인됨. 이 xlsx엔 실제 HKMC向 매출/매입/외주단가·품목코드·차종 매핑이 들어있어 공개 저장소에 올리면 영업기밀 노출. frappe-architect 서브에이전트가 이 파일을 포함해 push를 시도했으나 Claude Code 보안 분류기가 먼저 차단했고, 사용자 확인 후 커밋을 amend해 제외. **향후 원칙**: 실제 영업기밀(단가·고객 매핑 등)이 담긴 원본 데이터 파일은 이 저장소가 public인 한 절대 커밋하지 않는다 — 구조/스키마 설계 문서는 커밋해도 되지만 원본 데이터는 로컬 전용 |
| 2026.07.20 | 완제품 21건(전수검사) Quality Inspection Template 껍데기를 **fixtures가 아니라 patch**(`apps/babipa_erp/babipa_erp/patches/v0_0/create_finished_goods_qi_template_shells.py`)로 구현 | `hkmc-compliance` 리뷰의 "`custom_inspection_type`이 실제 검사기록 생성을 강제하지 않는 라벨뿐" 지적 보완. 실제 검사 파라미터(측정 항목/규격 상하한/AQL)를 담은 도면·Control Plan·검사기준서 원본이 아직 없다고 사용자가 명시 확인 — 값은 채우지 않고 품목코드당 템플릿 1건 + placeholder 파라미터 링크만 생성. `item_quality_inspection_parameter`(Table)가 `reqd=1`이라 빈 테이블은 저장 자체가 막힘을 개발 사이트 실측으로 확인(자식 테이블의 `specification`도 Data가 아니라 `Quality Inspection Parameter` Link이자 `reqd=1`), 그래서 21건이 공유하는 placeholder 마스터 파라미터 1건을 만들어 연결. fixtures 대신 patch를 택한 이유: 이 21건은 Item Group 같은 "앱이 항상 동일하게 유지해야 하는 분류 체계"가 아니라 품목코드 1:1 인스턴스 데이터이고, 품질부서가 도면 확보 후 직접 값을 채워 넣을 대상이라 `bench migrate` 시 fixtures로 재동기화되며 실제 입력값이 덮어써지거나 껍데기로 되돌아갈 위험을 피하기 위함(멱등성 가드 포함, 상세 근거는 `docs/item-doctype-design.md` §6.1). 실 Item 레코드가 아직 없어 `quality_inspection_template` 연결은 완제품 임포트 시점(frappe-backend)으로 위임 |
| 2026.07.20 | `docs/품목코드_260720.xlsx` 289건 실데이터를 Item 마스터로 실제 임포트(`apps/babipa_erp/babipa_erp/patches/v0_0/import_item_master_260720.py`, patch) — `docs/item-doctype-design.md` 설계·본 문서의 Serial/Batch·Item Group·stock_uom 확정을 그대로 코드화. **단가(매출/매입) 리터럴은 patch 코드에 넣지 않고, 런타임에 gitignore된 로컬 사본(`apps/babipa_erp/babipa_erp/patches/v0_0/data/item_master_260720.xlsx`)을 직접 읽는 방식으로 구현** — 사본이 없는 환경(공개 clone 등)에서는 조용히 스킵. stock_uom 공백은 "EA"로 일괄 기본값(이번에 사용자 확정), 189건 중 실제로는 185건이 공백이었고 나머지는 원본값 "EA"(103) 또는 "g"(1, 코팅액 품목)를 그대로 보존 — "EA"·"g" UOM 마스터가 사이트에 없어 patch가 함께 생성. 개발 사이트 `bench migrate`로 실행·검증 완료(검증 항목은 `docs/item-doctype-design.md` §9 참조) | `create_finished_goods_qi_template_shells.py`(품목코드·항목명만 포함, 상대적 저민감) 선례와 달리 이번 데이터엔 실제 원화 단가가 포함돼 있어, 그 리터럴을 patch 코드에 그대로 옮기면 "원본 데이터 파일은 public repo에 커밋하지 않는다"(위 2026.07.20 gitignore 결정)는 원칙을 코드로 형태만 바꿔 위반하는 셈이 된다고 판단 — 그래서 이번 patch만 "런타임 파일 읽기 + gitignore" 방식으로 구현 방식을 다르게 가져갔다. 이 판단은 사용자 지시(patch 구현, QI 껍데기 선례 따르기)의 문언을 문자 그대로 따르기보다, 이 저장소에 이미 확정된 상위 원칙(영업기밀 미커밋)을 우선한 것 — 후속 세션에서 재검토 필요하면 이 행을 참조 |
| 2026.07.20 | SMT·후가공·조립 제조/MES 데이터 기반 신설 — 커스텀 DocType 3개(`Equipment`/장비, `Equipment Daily Inspection`+child `Equipment Daily Inspection Item`/일일점검, `Production Downtime Log`/생산 다운타임 로그, 전부 `apps/babipa_erp/babipa_erp/babipa_erp/doctype/`) + Workstation Type·Operation(PP/PC/AS/QT) fixture 2건 + Workstation·Equipment·Equipment Daily Inspection·`TEST-` 접두사 전용 BOM/Item/Work Order/Job Card/Downtime Log patch 2건(`create_manufacturing_masters_260720.py`, `create_test_bom_production_data_260720.py`) | 사용자 요청("SMT/후가공/조립+장비+일일점검+MES+정전개/역전개") 대응. 실 289개 부품코드에는 실제 BOM 구조가 없어 가짜 자재구성을 붙이지 않고 `TEST-` 전용 Item Group/Item으로 완전히 분리(BOM 정전개·역전개는 Desk UI에서 직접 열어 확인 완료, 실 Item 개수 299건 불변도 재확인). Operation 4종(PP/PC/AS/QT)은 지어낸 분류가 아니라 Item 마스터 `custom_default_operation` 실측 분포(위 2026.07.20 Serial/Batch 행 참조)를 그대로 반영. 권한은 5-role 매트릭스(§5.4)가 아직 Role 레코드로 미구현(콘솔 확인, 0건)이라 ERPNext 표준 role(Manufacturing User/Manager, Quality Manager, System Manager)로 걸었음 — 5-role 전체 이관은 Item/BOM/Work Order 등 기존 문서까지 걸치는 별도 범위라 이번엔 보류. 교정성적서번호·제조사·모델 등은 실제 확인 전까지 공란/TBD 유지(QI템플릿 선례 원칙 그대로 적용). **부수 발견·정정**: `.claude/skills/frappe-doctype-json/SKILL.md`의 기본 골격이 `"custom": 1`을 지시하고 있었는데, 이 값이 1이면 `frappe.model.base_document.get_controller()`가 무조건 base `Document`를 반환해 앱 폴더의 실제 컨트롤러 클래스(`validate()` 등)가 전혀 로드되지 않는다는 것을 이번 3개 DocType에서 직접 재현·확인 — `custom: 0`으로 정정 후 컨트롤러 로딩·`Equipment Daily Inspection`의 이상판정 검증 로직·`Production Downtime Log`의 소요시간 자동계산 전부 정상 동작 재확인 |
| 2026.07.20 | 회계모듈 착수 우선순위 3번(CoA 검토) 완료 확정 — `BABIPA` Chart of Accounts(계정 94건, `Standard` 템플릿)는 부가세·전자세금계산서 등 세무신고 전용 계정 자체가 없는 범용 구성이라 §4.5(ERP=관리회계만, 세무는 더존 유지) 톤다운 방향과 이미 부합, 별도 조정 없이 그대로 사용하기로 확정. 이어서 4번(경리 role 실제 생성) 착수 — `경리` Role 신설 + `Journal Entry`·`Payment Entry`(Full RWCD+Submit/Cancel/Amend)·`GL Entry`(Read-only) Custom DocPerm 부여 | CoA는 "Tax Assets" 그룹이 하위 계정 없이 비어있는 정도 외에는 세무 특화 요소가 없어 검토할 대상 자체가 크지 않았음. 경리 권한 범위는 §5.4가 이미 명시한 "경리 role의 Submit 대상은 내부 정합용 전표에 한정"을 그대로 코드화한 것 — Journal Entry/Payment Entry는 경리가 직접 전표를 확정(Submit)하도록 했고(생산/품질/구매 실무진과 달리 팀장 승인을 거치지 않음, §5.4 원문 근거), GL Entry는 시스템이 자동 생성하는 원장이라 Read만 부여. Purchase/Sales Invoice·Supplier·Customer 등은 §5.4 DocType 목록에 경리 몫으로 명시되지 않아 이번엔 권한을 주지 않음 — 필요해지면 사용자 확인 후 추가. 상세는 `docs/build-log.md` 해당 항목 |
| 2026.07.20 | 회계모듈 착수 우선순위 5번(Track Changes) 완료 — §5.4 대상 7개 DocType 중 Item/BOM/Work Order/Journal Entry/Payment Entry 5개는 ERPNext 기본값이 이미 `track_changes=1`이라 그대로 두고, `Quality Inspection`·`GL Entry` 2개만 Property Setter로 켬 | 코어 DocType(`Quality Inspection`·`GL Entry`)이라 JSON 직접 수정은 "코어 미수정" 원칙 위반 — Customize Form UI가 쓰는 것과 동일한 `Property Setter` 오버레이 메커니즘으로 처리해 fixture로 버전관리. PPAP/APQP/CP/PFMEA/FAI/4M은 아직 DocType이 없어(로드맵 3단계 예정) 이번 범위에서 제외 — 나중에 만들 때 자체 JSON에 `track_changes: 1`을 바로 넣으면 되므로 Property Setter가 필요 없음 |
| 2026.07.20 | 회계모듈 착수 우선순위 6번(§5.5 PO·Payment Entry Workflow) 완료 — `Purchase Order`(발주요청→승인대기→확정)·`Payment Entry`(지급요청→승인대기→실행) 2개 Frappe Workflow 구현. 선행 조건으로 `관리자`(승인자, 두 DocType RWCD+Submit/Cancel/Amend)·`구매`(기안자, Purchase Order RWC만) Role 신규 생성, **경리 Role의 Payment Entry Submit/Cancel/Amend 권한 회수**(항목 4에서 부여했던 것을 0으로 되돌림) | Workflow를 실제로 구현하려면 승인자·기안자 역할이 있어야 하는데 §5.4의 5개 역할 중 경리 하나만 만들어져 있어 관리자·구매 신설이 이번 작업의 불가피한 선행조건이었음(생산·품질은 이번 워크플로우와 무관해 보류) — 두 Role의 권한 범위도 이번에 필요한 DocType(Purchase Order·Payment Entry)에만 한정, 나머지 DocType 매트릭스는 여전히 미착수. 경리 권한 회수는 Frappe 소스(`frappe/model/workflow.py`) 확인 결과 `apply_workflow()`가 `doc.submit()`을 permission 우회 없이 호출한다는 걸 알게 되면서 나온 필연적 정정 — 경리가 Payment Entry Submit 권한을 그대로 갖고 있으면 워크플로우 UI 버튼만 숨겨질 뿐 API로 직접 제출해 관리자 승인을 건너뛸 수 있는 구조였음. 항목 4 시점엔 아직 워크플로우가 없어 문제가 되지 않았던 설계가 이번 요구사항과 충돌한 것 — 사용자에게 별도 확인 없이 바로잡음(§5.5가 이미 확정한 승인 게이트 요구사항을 실제로 만족시키기 위한 수정이라 새로운 판단이 아니라고 보고 진행, 문제 있으면 알려달라고 응답에 명시). 승인·반려 전이는 `allow_self_approval=0`으로 설정해 기안자 본인이 자기 문서를 승인하지 못하게 막음. 실 Purchase Order 1건으로 전체 상태전이(발주요청→승인대기→확정, docstatus 0→0→1)를 실행 검증 후 정리 완료 |
| 2026.07.20 | 회계모듈 착수 우선순위 7번(재고평가 vs GL 대사 검증, `docs/decisions.md` §6.7 Go-live 게이트 원문 항목) 완료로 3~7번 전체 마무리 — 구조 확인(perpetual inventory 활성화, 창고별 계정 공백이지만 Company 기본 재고자산 계정으로 정상 폴백) + 실제 Material Receipt 1건 왕복 테스트로 Stock Ledger 평가액과 GL `Stock In Hand - B` 잔액이 전표 단위·회사 전체 양쪽 다 정확히 일치함을 실측 확인 | 실 289개 품목은 아직 입출고 이력이 0건이라 "지금 얼마가 맞는지"를 확인할 실측 데이터 자체가 없었음 — 그래서 이번 검증은 "메커니즘이 맞게 짜여 있는가"를 증명하는 성격으로 진행(TEST 품목으로 왕복 테스트 후 정리, 실 Item 데이터는 건드리지 않음). 창고 5건 전부 `account` 필드가 비어있다는 점 자체는 ERPNext `get_warehouse_account()` 폴백 로직 코드를 직접 확인해 Company 기본 계정으로 정상 대체됨을 검증했으므로 결함이 아니라고 판단 — 다만 창고 유형별(원재료/재공품/완제품)로 GL 계정을 분리해 관리하고 싶어지면 그때 각 창고에 `account`를 개별 지정하는 후속 작업이 필요함을 남겨둠(현재는 단일 계정으로 충분하다고 보고 변경하지 않음). 회계모듈 착수 우선순위(3번 CoA 검토 ~ 7번 재고-GL 대사)는 이번 항목으로 전부 완료, 남은 건 원래 §5.4가 정의했지만 아직 미착수인 생산·품질 Role과 그 전체 권한 매트릭스 |
| 2026.07.21 | §5.4 권한 Role Matrix 5개 역할(경리/관리자/구매/생산/품질) 구현 완료 — `생산`(Work Order/Job Card/Stock Entry RWC)·`품질`(Quality Inspection RWC) Role 신규 생성, `관리자`에게 같은 4개 DocType RWCD+Submit/Cancel/Amend 추가 부여 | Purchase Order·Payment Entry에 적용했던 "실무진 RWC, 확정은 관리자" 패턴(§5.4)을 §5.5가 Workflow 없이도 충분하다고 이미 지정해둔 나머지 DocType(Work Order/Job Card/Stock Entry/Quality Inspection)까지 그대로 확장한 것 — 새로운 판단이 필요한 지점은 아니었음. Item/BOM은 §5.4 원문이 5개 역할 중 담당을 특정하지 않아 이번에도 권한 부여 대상에서 제외(hooks.py 주석에 명시, 필요시 후속 확인). PPAP/APQP/CP/PFMEA/FAI/4M DocType이 아직 없어 품질 Role의 향후 확장 여지로 남겨둠. 이 항목으로 §5.4 Role Matrix 자체는 구조적으로 완결됐으나, 각 Role을 실제 사용자 계정에 배정하는 작업(User 생성·Role 부여)은 별도 범위 |
| 2026.07.21 | Item/BOM 담당 role 확정(사용자 확인) — **Item: 구매·생산 RWC**(둘 다 품목 마스터 작성 주체, 최소 2개 역할 부여 지시 반영). **BOM: 생산 RWC + 품질 Read-only + 관리자 Submit/Cancel/Amend** | 위 §5.4 행에서 미결정으로 남겨뒀던 항목을 사용자가 직접 지정. Item은 `is_submittable=0`이라 확정 개념이 없어 구매·생산 둘 다 대등하게 RWC — 구매는 원자재/구매품(외주·상품) Item, 생산은 자체 공정 커스텀필드(제조유형·검사구분·기본작업공정 등)가 딸린 Item과 BOM(제조 레시피)의 실제 작성 주체라는 실무 분담 근거. BOM은 `is_submittable=1`이라 기존 Purchase Order 등과 같은 패턴(실무진 RWC+관리자 확정)을 그대로 적용, 품질은 PFMEA/Control Plan 작업 시 BOM 구조를 참조만 하지 작성하지 않는다는 점에서 Read-only로 차등 부여(요청한 "최소 2개 역할"은 만족하되 셋 다 동일 권한을 주지는 않음 — 판단 근거를 남겨 필요시 재조정 가능하도록 함). 부수적으로 구매 role이 그동안 Item에 아무 권한이 없어 Purchase Order 작성 시 Item Link 필드 선택이 막혀 있었을 잠재적 결함도 이번에 함께 해소 |
| 2026.07.21 | PPAP/APQP Gate/Control Plan/PFMEA/FAI/4M Change 6개 신규 DocType 구축(로드맵 3단계를 §5.4 매트릭스 완성과 함께 앞당김, `apps/babipa_erp/babipa_erp/babipa_erp/doctype/{ppap_submission,apqp_gate,control_plan,pfmea,fai,4m_change}/`) + §5.4 매트릭스를 Sales Order/Delivery Note/Stock Ledger Entry/Purchase Receipt/Supplier + 이 6개 신규 DocType까지 확장(생산·품질·구매·관리자 Custom DocPerm 24행 추가) | 이 세션 착수 시점에 §5.4 나머지(Item/BOM/Work Order/Job Card/Stock Entry/Quality Inspection)는 **동시에 진행 중이던 다른 세션**이 이미 구현·커밋 완료한 상태였음(커밋 `f9584e6`·`d2aaf52`, 같은 저장소·같은 개발 DB를 공유하는 별도 세션으로 확인 — `ps aux`에 남은 프로세스는 없었으나 hooks.py 주석·git log·Custom DocPerm 생성시각이 이를 증명). `frappe.permissions.add_permission`이 기존 행이 있으면 스킵하는 멱등성 덕분에 두 세션의 작업이 충돌 없이 병합됨(`custom_docperm.json` 재수출 결과 624줄 순수 추가, 삭제 0) — role.json은 변경 없음(생산·품질 Role이 이미 그 세션에서 fixture로 커밋돼 있었음). PPAP/FAI/4M Change는 실측값·제출이력을 지어내지 않고 스키마만 구축(QI템플릿 껍데기 선례). "4M Change"는 숫자로 시작해 Python 클래스명이 될 수 없어(`doctype.replace(" ","").replace("-","")`가 컨트롤러 조회 키) `class FourMChange`를 정의한 뒤 `globals()["4MChange"] = FourMChange`로 모듈 네임스페이스에 등록하는 방식으로 해결 — 지난 세션의 `custom:1` 버그와 같은 종류의 "실제로 검증하지 않으면 조용히 깨지는" 문제라 `bench console`에서 `get_controller()`로 9개 DocType 전부 재확인. System Manager 2인 이내 배정(§5.4)의 실제 인원 지정은 이번에도 보류(구조만) |
| 2026.07.21 | §5.4를 5개 역할에서 6개로 확장 — **`평가`(시험평가/Validation) Role 신규 추가**, System Manager 2인 이내(§5.4) 중 첫 번째 자리를 평가 Role에 배정. 역할 기반 테스트 User 계정 6건 생성(`accounting@babipa.test`→경리, `manager@babipa.test`→관리자, `purchasing@babipa.test`→구매, `production@babipa.test`→생산, `quality@babipa.test`→품질, `evaluation@babipa.test`→평가+System Manager) | 사용자가 직접 지시: "현재 개발하는 내가 평가"라는 확인에 따라, 실제 이 프로젝트를 만들고 있는 사람의 소속이 시험평가팀이므로 그 업무 성격을 반영하는 별도 역할을 만들고 그 역할에 먼저 System Manager를 겸임시킴(두 번째 자리는 사용자가 명시적으로 미정으로 남김). User 계정은 실제 담당자 이름·이메일이 없어 역할 기반 테스트 계정(도메인 `babipa.test`)으로 생성 — 사용자가 두 정체성(계정 식별자 방식, System Manager 배정처) 질문 모두에 답한 대로 반영. User 자체는 Role/Custom DocPerm과 달리 fixture·patch로 코드화하지 않음(상세 근거는 `docs/build-log.md` 해당 항목 — public repo에 테스트 비밀번호를 고정 노출하지 않기 위함, Company/Fiscal Year 등 Setup Wizard급 사이트 데이터를 fixture로 안 두는 기존 선례와 동일 원칙). `평가` Role 자체(이름만)는 다른 5개 Role과 동일하게 fixture로 관리 — 아직 DocType별 권한은 부여하지 않음(§5.4 원래 매트릭스에 없던 역할이라 권한 범위는 별도 확인 필요) |
